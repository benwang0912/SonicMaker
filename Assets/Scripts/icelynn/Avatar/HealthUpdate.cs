using UnityEngine;
using System.Collections;

public class HealthUpdate : MonoBehaviour {

	float timer_f = 0f;
	int timer_i = 0;
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//timer_i = (int)Time.time / 10;
		//ScoreCount.health -= (timer_i * 10);
	}
}
